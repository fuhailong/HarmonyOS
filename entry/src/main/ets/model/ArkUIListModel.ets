export class ArkUIListModel {
  id: number
  title: string
  subTitle: string

  constructor(id: number, title: string, subTitle: string) {
    this.id = id
    this.title = title
    this.subTitle = subTitle
  }
}